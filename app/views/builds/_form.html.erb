<%= form_for(@build) do |f| %>
  <% if @build.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@build.errors.count, "error") %> prohibited this build from being saved:</h2>

      <ul>
      <% @build.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :build_number %><br />
    <%= f.number_field :build_number %>
  </div>
  <div class="field">
    <%= f.label :total_test %><br />
    <%= f.number_field :total_test %>
  </div>
  <div class="field">
    <%= f.label :skipped_test %><br />
    <%= f.number_field :skipped_test %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
